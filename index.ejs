<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>To-Do List</title>
    <style>
        body{
            font-family: 'Times New Roman', Times, serif;
            height: 1000px;
            background-image:-webkit-linear-gradient(63deg, rgba(128, 0, 128, 0.641) 50%, rgba(249, 176, 254, 0.252) 50%);
        }

        h1{
            text-align: center;
        }

        .list_contaner{
            display: flex;
            justify-content: center;
            align-items: center;
        }
        ul{
            display: flex;
            height: auto;
            max-width: 790px;
            padding: 0;
           flex-wrap: wrap;
        }
        
        li{
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: row;
            height:150px;
            min-width:200px;
            background-color: rgb(244, 242, 242);
            margin: 20px;
            text-align: center;
            border:1px solid gray;
            border-radius: 8px;

        }

        li a{
            text-decoration: none;
            color: black;
            font-size: 18px;
            font-weight: 600;
        }
        .addlist_contaren{
            background-color: rgba(126, 21, 163, 0.927);
            display: flex;
            align-items: center;
            justify-content: center;
            height: 50px;
            width: 50px;
            border-radius: 50%;
            position: fixed;
            bottom: 0;
            right: 0;
            margin: 35px 25px;
            box-shadow: 0px 0px 5px gray;
           
        
        }
        .addlist{
            text-align: center;
            text-decoration: none;            
            color:white;
            font-size: 30px;
            font-weight: 700;

        }
    </style>

</head>
<body>
    <h1>To-Dob Lists</h1>
    <div class="list_contaner">
    <ul>
        <% lists.forEach(list => { %>
            <li><a href="/list/<%= list.id %>"><%= list.name %></a></li>
        <% }); %>
    </ul>
</div>

    <div class="addlist_contaren">
    <a class="addlist" href="/add-list">+</a>
</div>
</body>
</html>

